import { describe, it, expect } from '@jest/globals'
import 'reflect-metadata'

describe('MongoConnPool', () => {
  it('provideds a connected mongo client', () => {
    expect(false).toBeTruthy()
  })
  it('can clean all connections', () => {
    expect(false).toBeTruthy()
  })
})
